FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 安装最新版本的AKShare和AKTools
RUN pip install --no-cache-dir akshare==1.17.46 aktools==0.0.90

# 暴露端口
EXPOSE 8080

# 启动AKTools
CMD ["python", "-m", "aktools", "--host", "0.0.0.0", "--port", "8080"]